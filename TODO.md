# TODO - Fix Errors in Lebian-Project\n\n## Plan Steps:\n1. [x] Edit package.json: Move bcrypt and @types/bcrypt from devDependencies to dependencies.\n2. [x] Created .env.example with MONGODB_URI template for Atlas.\n3. [ ] Run `cd Backend-Lebian && npm install` to update node_modules.\n4. [ ] Copy .env.example to .env, add your Atlas MONGODB_URI and other secrets.\n5. [ ] Run `cd Backend-Lebian && npm run start:dev` to test backend.\n6. [ ] Frontend should work once backend runs on http://localhost:3000.\n\n**Next: Run the commands above.**
